{{#each posts as |post| }}
<div class="post">
    <div>
        <h2>
            <a href="/post/{{post.id}}">{{post.name}}</a>
        </h2>
        <p>Posted by {{post.user.name}} on {{format_date post.date_created}}</p>
    </div>
    <div>
        <p>
            {{post.content}}
        </p>
    </div>
</div>
{{/each}}