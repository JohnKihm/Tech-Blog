<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Your Dashboard</title>
</head>

<body>
    <div class="flex-col justify-around h-dvh">
        <header class="flex-col justify-between items-center p-2 bg-emerald-800">
            <nav class="text-xl text-cyan-200 flex justify-between max-w-md p-2 mx-3">
                <a href="/">Home</a>
                <a href="/dashboard">Dashboard</a>
                <!-- Users must be logged in to access dashboard so logout button is always rendered -->
                <button id="logout">Logout</button>
            </nav>
            <div class="flex p-2 mx-3">
                <h1 class="text-4xl text-emerald-800 font-bold bg-cyan-200 p-2 rounded-lg">Your Dashboard</h1>
            </div>
        </header>
        <main class="h-full bg-cyan-200 pt-3">
            <!-- Render the sub layout -->
            {{{ body }}}
        </main>
    </div>
    <!-- Users must be logged in to access dashboard so logout script is always rendered -->
    <script src="/js/logout.js"></script>
</body>

</html>